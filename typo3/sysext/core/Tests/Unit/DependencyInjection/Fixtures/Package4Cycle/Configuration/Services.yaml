services:

  package4.listener:
    class: stdClass
    tags:
      - { name: event.listener,
          event: TYPO3\CMS\Core\Foo\Event\TestEvent,
          before: 'legacy-hook',
          after: 'legacy-hook' }

