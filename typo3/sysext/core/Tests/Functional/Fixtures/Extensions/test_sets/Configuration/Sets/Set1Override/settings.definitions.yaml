categories:
  foo:
    label: 'Foo Override'

settings:
  foo.bar:
    type: string
    label: 'Foo Bar Override'
    default: ''
    category: foo
    description: |
      Hallo Welt

      > foo
      > bar
