# Fully cached page, no plugin
config.pageTitleProviders {
  testMetaProvider {
    provider = TYPO3Tests\TestMeta\PageTitle\CustomPageTitleProvider
    before = record
  }
}

page = PAGE
page.5 = TEXT
page.5.value = MetaData-Test
page.5.stdWrap.wrap = <h1>|</h1>
