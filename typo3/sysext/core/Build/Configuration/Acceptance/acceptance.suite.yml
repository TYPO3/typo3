class_name: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - \Helper\Acceptance
        - Asserts
        - \Helper\Unit
    config:
      WebDriver:
        url: http://localhost:8000
        browser: phantomjs
        http_proxy: direct

env:
    firefox:
        modules:
            config:
                WebDriver:
                    browser: 'firefox'
                    window_size: 1024x768
    phantom:
        # nothing changed