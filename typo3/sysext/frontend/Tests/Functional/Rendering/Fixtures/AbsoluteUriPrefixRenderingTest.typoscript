page = PAGE
page {
  includeCSS {
    extensionCSS = {$extensionCSS}
    externalCSS = {$externalCSS}
  }

  includeJS {
    extensionJS = {$extensionJS}
    externalJS = {$externalJS}
  }
  # added so we can also test the inclusion of these types in FE
  includeCSSLibs {
    extensionCSS = {$extensionCSS}
  }
  includeJSLibs {
    extensionJS = {$extensionJS}
  }
  includeJSFooterlibs {
    extensionJS = {$extensionJS}
  }
  10 = TEXT
  10.value = Link to page
  10.typolink.parameter = 10
  20 = IMAGE
  20 {
    file = {$localImage}
  }
  30 = TEXT
  30.value = Link to img_resource
  30.typolink {
    parameter.cObject = IMG_RESOURCE
    parameter.cObject.file = {$falImage}
  }
}

[traverse(request.getQueryParams(), 'useAbsoluteUrls') == '1']
  config.forceAbsoluteUrls = 1
[GLOBAL]

[traverse(request.getQueryParams(), 'useAbsoluteUrls') == '2']
  page.30.typolink.forceAbsoluteUrl = 1
[GLOBAL]
