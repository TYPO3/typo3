config {
    no_cache = 1
    debug = 0
    admPanel = 0
    sendCacheHeaders = 0
}

page = PAGE
page {
    typeNum = 0
    10 = TEXT
    10.value = Test
}

// dummy condition for forge issue #95882
[siteLanguage("languageId") == 10 || 10 in siteLanguage("fallbackLanguageIds")]
  // dummy condition to check if it can be evaluated in RedirectService::getTargetUrl()
  page.10.value = LanguageId 10  or Fallback Language Ids
[end]
